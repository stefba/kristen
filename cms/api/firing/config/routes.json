{
  "routes": [
    {
      "method": "GET",
      "path": "/firings",
      "handler": "firing.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/firings/count",
      "handler": "firing.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/firings/:id",
      "handler": "firing.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/firings",
      "handler": "firing.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/firings/:id",
      "handler": "firing.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/firings/:id",
      "handler": "firing.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
